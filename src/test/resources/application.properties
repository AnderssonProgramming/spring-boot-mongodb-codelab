# Fake local URI used only so the Spring context loads in tests
# without depending on the MONGODB_URI environment variable or a real cluster.
spring.data.mongodb.uri=mongodb://localhost:27017/test_db
